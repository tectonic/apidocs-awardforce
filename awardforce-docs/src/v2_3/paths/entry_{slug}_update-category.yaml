put:
  summary: Update entry category
  tags:
    - Entry (realtime)
  parameters:
    - $ref: ../components/parameters/Accept.yaml
  responses:
    default:
      description: Update entry category
  requestBody:
    content:
      application/json:
        schema:
          type: object
parameters:
  - name: slug
    in: path
    required: true
    schema:
      type: string
