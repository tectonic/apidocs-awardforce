description: |-
  Resource identified by the path slug does not exist. Returned when the slug is well-formed but no record matches it.
headers:
  X-RateLimit-Limit:
    $ref: ../headers/X-RateLimit-Limit.yaml
  X-RateLimit-Remaining:
    $ref: ../headers/X-RateLimit-Remaining.yaml
  X-RateLimit-Reset:
    $ref: ../headers/X-RateLimit-Reset.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/ProblemTypes/NotFound.yaml
