description: Entry title update payload.
content:
  application/json:
    schema:
      type: object
      additionalProperties: false
      required:
        - title
      properties:
        title:
          type: string
          maxLength: 255
          description: New title of the entry.
required: true