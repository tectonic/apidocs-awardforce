name: order
in: query
required: false
description: Sort by a supported order field.
schema:
  type: string
  enum:
    - date
    - email
    - first_name
    - last_name
    - local_id
    - number
    - payment_method
    - payment_status
    - season
    - total
