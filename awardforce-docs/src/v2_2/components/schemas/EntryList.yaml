description: Paginated list of entries.
allOf:
  - $ref: ./Pagination.yaml
  - type: object
    properties:
      data:
        type: array
        description: Entries on the current page.
        items:
          $ref: ./EntrySummary.yaml
