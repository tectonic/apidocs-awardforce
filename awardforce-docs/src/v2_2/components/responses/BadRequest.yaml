description: |-
  Request was rejected before the endpoint could process it. Common causes: invalid `Accept` header, unsupported `x-api-language` code, empty request body on `POST` / `PUT`, invalid JSON in the request body, or an invalid slug format in a path parameter.
headers:
  X-RateLimit-Limit:
    $ref: ../headers/X-RateLimit-Limit.yaml
  X-RateLimit-Remaining:
    $ref: ../headers/X-RateLimit-Remaining.yaml
  X-RateLimit-Reset:
    $ref: ../headers/X-RateLimit-Reset.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/ProblemTypes/BadRequest.yaml
