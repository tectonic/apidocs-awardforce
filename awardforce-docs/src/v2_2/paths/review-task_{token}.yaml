get:
  summary: Get review task
  tags:
    - Review task
  parameters:
    - $ref: ../components/parameters/Accept.yaml
  responses:
    default:
      description: Get review task
  requestBody:
    content:
      application/json:
        schema:
          type: object
parameters:
  - name: token
    in: path
    required: true
    schema:
      type: string
