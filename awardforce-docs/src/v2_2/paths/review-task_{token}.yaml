get:
  summary: Get review task
  tags:
    - Review task
  parameters:
    - name: Accept
      in: header
      required: true
      description: Defines the response type.
      schema:
        type: string
  responses:
    default:
      description: Get review task
  requestBody:
    content:
      application/json:
        schema:
          type: object
parameters:
  - name: token
    in: path
    required: true
    schema:
      type: string
