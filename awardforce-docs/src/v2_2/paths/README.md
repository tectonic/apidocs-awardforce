Paths
=====

- Paths are plural.
- Write each path specification in a separate file.
- Filename is mapped to a path by using `_` as a path separator while preserving `{param}` tokens, for example `user_{slug}.yaml` matches `/user/{slug}`.
