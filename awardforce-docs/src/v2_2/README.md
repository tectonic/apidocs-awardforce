Versioned OpenAPI source
===========

- Keep the version entrypoint in `openapi.yaml`.
- Keep reusable version-local components in `components/`.
- Keep path specifications in `paths/`.
- Bundle this version into `../../api-reference/openapi-v2_2.yaml`.
