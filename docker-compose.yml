x-build-base: &build-base
  build:
    context: .
    args:
      USER: ${USER}
      UID: ${UID}
      GID: ${GID}

x-mint-base: &mint-base
  <<: *build-base
  stdin_open: true
  tty: true
  restart: "no"
  volumes:
    - .:/docs
  environment:
    - NODE_ENV=development

services:
  mint-awardforce:
    <<: *mint-base
    working_dir: /docs/awardforce-docs
    command: /docs/node_modules/.bin/mint dev --port 3000 --no-open
    ports:
      - "${MINT_PORT_AWARDFORCE:-3000}:3000"

  mint-goodgrants:
    <<: *mint-base
    working_dir: /docs/goodgrants-docs
    command: /docs/node_modules/.bin/mint dev --port 3000 --no-open
    ports:
      - "${MINT_PORT_GOODGRANTS:-3001}:3000"

  npm:
    <<: *build-base
    working_dir: /docs
    volumes:
      - .:/docs
    entrypoint: ["npm"]

