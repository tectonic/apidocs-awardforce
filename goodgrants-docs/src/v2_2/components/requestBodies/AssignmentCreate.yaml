content:
  application/json:
    schema:
      type: object
      required:
        - score_set
        - applications
        - reviewers
        - rounds
      properties:
        score_set:
          type: string
          description: Slug of the score set.
        applications:
          type: array
          description: List of application slugs.
          items:
            type: string
        reviewers:
          type: array
          description: List of reviewer slugs.
          items:
            type: string
        rounds:
          type: array
          description: List of round slugs.
          items:
            type: string
description: Assignment creation payload.
required: true