name: order
in: query
required: false
description: |-
  Sort by a supported application field.
schema:
  type: string
  enum:
    - category
    - chapter
    - applicant
    - local_id
    - parent_category
    - title
    - updated
