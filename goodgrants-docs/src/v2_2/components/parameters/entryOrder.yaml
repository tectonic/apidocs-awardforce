name: order
in: query
required: false
description: |-
  Sort by a supported entry field.
  Supported values: category, chapter, applicant, local_id, parent_category, title, updated.
schema:
  type: string
  enum:
    - category
    - chapter
    - applicant
    - local_id
    - parent_category
    - title
    - updated
