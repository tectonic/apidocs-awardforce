parameters:
  - name: slug
    in: path
    required: true
    schema:
      type: string
  - $ref: ../components/parameters/fieldSlug.yaml
put:
  summary: Update entry field
  operationId: PutEntryUpdateFieldV22
  tags:
    - Entry (realtime)
  parameters:
    - $ref: ../components/parameters/Accept.yaml
  responses:
    default:
      description: Update entry field
  requestBody:
    content:
      application/json:
        schema:
          type: object
