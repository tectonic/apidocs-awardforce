get:
  summary: Get account
  tags:
    - Account
  parameters:
    - $ref: ../components/parameters/Accept.yaml
    - $ref: ../components/parameters/X-Api-Language.yaml
  responses:
    default:
      description: Account retrieved.
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Account.yaml
  description: Retrieves account information for the account associated with the API key in the request header.
