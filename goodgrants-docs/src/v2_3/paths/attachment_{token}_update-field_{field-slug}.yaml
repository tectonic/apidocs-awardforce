put:
  summary: Update entry attachment field
  tags:
    - Entry (realtime)
  parameters:
    - name: Accept
      in: header
      required: true
      description: Defines the response type.
      schema:
        type: string
  responses:
    default:
      description: Update entry attachment field
  requestBody:
    content:
      application/json:
        schema:
          type: object
parameters:
  - name: token
    in: path
    required: true
    schema:
      type: string
  - name: field-slug
    in: path
    required: true
    schema:
      type: string
