name: order
in: query
required: false
description: Sort by a supported order field.
schema:
  type: string
  enum:
    - created_at
    - invoice_number
    - payment_method
    - payment_status
    - season
    - total
