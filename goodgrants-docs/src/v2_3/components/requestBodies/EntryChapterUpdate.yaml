description: Entry chapter update payload.
content:
  application/json:
    schema:
      type: object
      additionalProperties: false
      required:
        - chapter
      properties:
        chapter:
          type: string
          pattern: '^[A-Za-z]{8}$'
          description: Slug of the chapter.
required: true