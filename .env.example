# Optional Mint dev server port overrides.
MINT_PORT_AWARDFORCE=3000
MINT_PORT_GOODGRANTS=3001

# If you run raw `docker compose` commands instead of `./docs`, you can also
# define these host identity values manually:
# HOST_USER=youruser
# HOST_UID=youruid
# HOST_GID=yourgid
